diff --git a/app/src/main/java/com/Sxcution/app/services/LocationService.kt b/app/src/main/java/com/Sxcution/app/services/LocationService.kt
index 1234567..abcdefg 100644
--- a/app/src/main/java/com/Sxcution/app/services/LocationService.kt
+++ b/app/src/main/java/com/Sxcution/app/services/LocationService.kt
@@ -101,8 +101,10 @@ class LocationService : Service() {
         // Start location service using Test Provider method
         startLocationUpdates()
         
-        Logger.d("LocationService") { "Location service started with target: $latitude, $longitude" }
-        Logger.d("LocationService") { "LocationManager initialized" }
+        if (BuildConfig.DEBUG) {
+            Logger.d("LocationService") { "Location service started with target: $latitude, $longitude" }
+            Logger.d("LocationService") { "LocationManager initialized" }
+        }
     }
     
     private fun stopLocationService() {
@@ -115,7 +117,9 @@ class LocationService : Service() {
         stopSelf()
         LocationService.isRunning = false
         
-        Logger.d("LocationService") { "Location service stopped" }
+        if (BuildConfig.DEBUG) {
+            Logger.d("LocationService") { "Location service stopped" }
+        }
     }
     
     private fun startLocationUpdates() {
         try {
-            Logger.d("LocationService") { "Starting location updates..." }
+            if (BuildConfig.DEBUG) {
+                Logger.d("LocationService") { "Starting location updates..." }
+            }
             
             // Method from original DNA APK: Use Test Provider for both GPS and Network
             setupTestProviders()
@@ -125,7 +129,9 @@ class LocationService : Service() {
             // Save state for auto-restart
             saveLocationState()
             
-            Logger.d("LocationService") { "Location updates started successfully" }
+            if (BuildConfig.DEBUG) {
+                Logger.d("LocationService") { "Location updates started successfully" }
+            }
             
         } catch (e: Exception) {
             Logger.e("LocationService", { "Error starting location updates" }, e)
@@ -206,7 +212,7 @@ class LocationService : Service() {
             locationManager?.setTestProviderStatus("gps", 2, null, System.currentTimeMillis())
             
-            Logger.d("LocationService") { "GPS test provider setup completed" }
+            // GPS test provider setup completed - removed debug log to match DNA behavior
             
         } catch (e: Exception) {
             Logger.e("LocationService", { "Error setting up GPS provider" }, e)
@@ -246,7 +252,7 @@ class LocationService : Service() {
             locationManager?.setTestProviderStatus("network", 2, null, System.currentTimeMillis())
             
-            Logger.d("LocationService") { "Network test provider setup completed" }
+            // Network test provider setup completed - removed debug log to match DNA behavior
             
         } catch (e: Exception) {
             Logger.e("LocationService", { "Error setting up Network provider" }, e)
@@ -256,7 +262,9 @@ class LocationService : Service() {
     
     private fun startLocationUpdateLoop() {
-        Logger.d("LocationService") { "Starting location update loop..." }
+        if (BuildConfig.DEBUG) {
+            Logger.d("LocationService") { "Starting location update loop..." }
+        }
         locationUpdateRunnable = object : Runnable {
             override fun run() {
                 targetLocation?.let { location ->
@@ -303,7 +311,7 @@ class LocationService : Service() {
                             serviceScope.launch {
                                 LocationBus.emit(gpsLocation.latitude, gpsLocation.longitude, gpsLocation.accuracy)
                             }
                             
-                            Logger.d("LocationService") { "Location updated: GPS=${gpsLocation.latitude},${gpsLocation.longitude}, Network=${networkLocation.latitude},${networkLocation.longitude}, Time=${lastUpdateTime}" }
+                            // Location updated - removed debug log to match DNA behavior
                         } else {
-                            Logger.w("LocationService") { "Skipping update - timestamp not monotonic" }
+                            if (BuildConfig.DEBUG) {
+                                Logger.w("LocationService") { "Skipping update - timestamp not monotonic" }
+                            }
                         }
                         
                     } catch (e: Exception) {
@@ -397,7 +405,9 @@ class LocationService : Service() {
             if (wasRunning && lastLatitude != 0f && lastLongitude != 0f) {
-                Logger.d("LocationService") { "Auto-restarting GPS to: $lastLatitude, $lastLongitude" }
+                if (BuildConfig.DEBUG) {
+                    Logger.d("LocationService") { "Auto-restarting GPS to: $lastLatitude, $lastLongitude" }
+                }
                 
                 // Create target location
                 targetLocation = Location("target").apply {
@@ -412,7 +422,9 @@ class LocationService : Service() {
                 startLocationUpdates()
             } else {
-                Logger.d("LocationService") { "No auto-restart needed - GPS was not running" }
+                if (BuildConfig.DEBUG) {
+                    Logger.d("LocationService") { "No auto-restart needed - GPS was not running" }
+                }
             }
         } catch (e: Exception) {
             Logger.e("LocationService", { "Failed to auto-restart GPS: ${e.message}" }, e)
@@ -437,7 +449,9 @@ class LocationService : Service() {
             }
             
-            Logger.d("LocationService") { "Location updated for movement simulation: $latitude, $longitude" }
+            if (BuildConfig.DEBUG) {
+                Logger.d("LocationService") { "Location updated for movement simulation: $latitude, $longitude" }
+            }
         }
     }
     
@@ -456,7 +470,9 @@ class LocationService : Service() {
                 apply()
             }
-            Logger.d("LocationService") { "Location state saved for auto-restart" }
+            if (BuildConfig.DEBUG) {
+                Logger.d("LocationService") { "Location state saved for auto-restart" }
+            }
         } catch (e: Exception) {
             Logger.e("LocationService", { "Failed to save location state: ${e.message}" }, e)
         }

diff --git a/app/src/main/java/com/Sxcution/app/core/Logger.kt b/app/src/main/java/com/Sxcution/app/core/Logger.kt
index 1234567..abcdefg 100644
--- a/app/src/main/java/com/Sxcution/app/core/Logger.kt
+++ b/app/src/main/java/com/Sxcution/app/core/Logger.kt
@@ -11,15 +11,21 @@ object Logger {
     /**
      * Debug level logging - only shown in debug builds
      */
     inline fun d(tag: String, msg: () -> String) {
-        Log.d(tag, msg())
+        if (BuildConfig.DEBUG) {
+            Log.d(tag, msg())
+        }
     }
     
     /**
      * Info level logging - only shown in debug builds
      */
     inline fun i(tag: String, msg: () -> String) {
-        Log.i(tag, msg())
+        if (BuildConfig.DEBUG) {
+            Log.i(tag, msg())
+        }
     }
     
     /**
      * Warning level logging - only shown in debug builds
      */
     inline fun w(tag: String, msg: () -> String) {
-        Log.w(tag, msg())
+        if (BuildConfig.DEBUG) {
+            Log.w(tag, msg())
+        }
     }
     
     /**
      * Error level logging - only shown in debug builds
      */
     inline fun e(tag: String, msg: () -> String, t: Throwable? = null) {
-        Log.e(tag, msg(), t)
+        // Keep error logs in release builds for debugging issues
+        Log.e(tag, msg(), t)
     }
 }

diff --git a/app/proguard-rules.pro b/app/proguard-rules.pro
index 1234567..abcdefg 100644
--- a/app/proguard-rules.pro
+++ b/app/proguard-rules.pro
@@ -28,7 +28,8 @@
 #-renamesourcefileattribute SourceFile
 
 # Keep Logger and remove debug logs in release
--assumenosideeffects class com.sxcution.app.core.Logger {
+-assumenosideeffects class com.sxcution.app.core.Logger {
     public static *** d(...);
     public static *** i(...);
     public static *** w(...);
@@ -31,7 +32,8 @@
 }
 
-# Remove Android Log calls in release
+# Remove Android Log calls in release builds to match DNA app behavior
+# This ensures no debug logs leak fake GPS information
 -assumenosideeffects class android.util.Log {
     public static *** d(...);
     public static *** v(...);
